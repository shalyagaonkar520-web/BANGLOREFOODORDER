import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.momsmagic.delivery',
  appName: 'MomsmagicDelivery',
  webDir: 'dist'
};

export default config;
